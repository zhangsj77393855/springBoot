#2.配置相关信息
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ssm_db?serverTimezone=UTC
    username: root
    password: root

#设置Mp相关的配�?
mybatis-plus:
  global-config:
    db-config:
      table-prefix: tbl_
